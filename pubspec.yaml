name: simple_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.0.0
  mobile_scanner: ^5.0.0
  flutter_contacts: ^1.1.9+2
  permission_handler: ^10.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/contacts.png"
  min_sdk_android: 21 

flutter:
  uses-material-design: true
